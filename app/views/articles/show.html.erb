<div class="card">
  <div class="card-header">
    <%= @article.title %>
  </div>
  <div class="card-body">    
    <p class="card-text"><%= simple_format(@article.description) %></p>
    <%= link_to "Edit this article", edit_article_path(@article), class: "btn btn-primary btn-sm" %>
  </div>
</div><br/>
<%= link_to "< Back to article list", articles_path, class: "btn btn-secondary" %>